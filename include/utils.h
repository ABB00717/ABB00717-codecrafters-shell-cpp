#ifndef UTILS_H
#define UTILS_H

#include <cstdio>
#include <filesystem>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>

bool inputCommand(std::string& input);
std::string getPath(std::string command);

#endif